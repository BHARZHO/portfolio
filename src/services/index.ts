/**
 * Services layer for external API integrations (e.g., contact form, GitHub integration, etc.)
 */

export const submitContactForm = async (data: unknown) => {
  // Placeholder service function for future contact form submission API
  console.log('Sending contact form data:', data);
  return { success: true };
};
